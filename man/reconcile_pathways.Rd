% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile.R
\name{reconcile_pathways}
\alias{reconcile_pathways}
\title{Reconcile per-comparison pathway picks across comparisons}
\usage{
reconcile_pathways(all_tables)
}
\arguments{
\item{all_tables}{Named list of \verb{_all} candidate data frames, one per
comparison (names are the comparison labels). Each needs \code{Group_Pw},
\code{Genes}, \code{hierarchy_n}, \code{Combined.Score.mean}, \code{Pathway}.}
}
\value{
Named list, same names as \code{all_tables}: each comparison's
reconciled one-pathway-per-gene-set table. Returns \code{all_tables}
unchanged when fewer than two comparisons are supplied.
}
\description{
\code{\link[=enrich_network]{enrich_network()}} writes each comparison a \emph{provisional} one-pathway-
per-gene-set table, chosen without looking at the other comparisons.
Given every comparison's \verb{_all} candidate table, this re-picks -- for
each \verb{(comparison, gene set)} -- the pathway that recurs across the most
comparisons (tie -> shallower hierarchy -> higher combined score ->
name), so the same biology lands under the same label everywhere and the
combined heatmaps line up.
}
\details{
Ported from
\code{Network_generation/R/network_enrichment_and_vis.R::reconcile_pathway_selection()},
with the folder I/O split into \code{\link[=reconcile_pathways_dir]{reconcile_pathways_dir()}}.
}
