% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-network.R
\name{plot_network}
\alias{plot_network}
\title{Build the interactive HTML network widget}
\usage{
plot_network(
  nodes,
  edges,
  title,
  clusters = NULL,
  colour_by = "pathway",
  theme = networkplot_theme()
)
}
\arguments{
\item{nodes}{Data frame with columns \code{Protein}, \code{type}, \code{prize},
\code{LogFC}, \code{degree}; optional \code{LogFC_all} (hover label) and \code{pathway}
(comma-joined pathway names -- enables the pathway multi-select when
\code{colour_by = "pathway"}).}

\item{edges}{Data frame with columns \code{from}, \code{to} (and, for
\code{theme$edge_width}, whatever column it names -- typically \code{cost} or
\code{weight}).}

\item{title}{Network title, shown as the widget heading.}

\item{clusters}{Optional data frame with columns \code{id}, \code{cluster}
(a \code{networkGen} result's \code{wc_df}); merged onto the nodes for reference.}

\item{colour_by}{\code{"pathway"} (default) enables the pathway multi-select
dropdown when a \code{pathway} column is present; anything else leaves it
off.}

\item{theme}{A \code{\link[=networkplot_theme]{networkplot_theme()}} object.}
}
\value{
A \code{visNetwork} htmlwidget.
}
\description{
Ports the interactive \code{visNetwork} rendering that lived in
\code{Network_generation/R/network_enrichment_and_vis.R::visualize_network_pg()},
with every hardcoded style choice moved onto \code{\link[=networkplot_theme]{networkplot_theme()}}.
}
\details{
Node fill is a diverging colour scale on \code{LogFC} (clamped to a symmetric
limit -- see \code{theme$logfc_limit_fn}). Node shape is looked up by \code{type}
in \code{theme$shapes}. Nodes with \code{degree >= theme$highlight_degree} get a
distinct border. Edge boldness is constant unless \code{theme$edge_width} maps
a per-edge column (e.g. \code{cost}) onto width.
}
