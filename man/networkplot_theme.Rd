% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{networkplot_theme}
\alias{networkplot_theme}
\title{Styling configuration for \code{\link[=plot_network]{plot_network()}}}
\usage{
networkplot_theme(
  shapes = NULL,
  shape_default = "ellipse",
  logfc_palette = c("#0000CC", "#BDBDBD", "#C62828"),
  logfc_limit_fn = NULL,
  highlight_degree = 5,
  highlight_border = "#4C9900",
  border_default = "#454545",
  highlight_border_width = 2.5,
  border_width_default = 1,
  edge_colour = "#000000",
  edge_width = NULL,
  node_font_size = 10,
  legend_font_size = 12,
  layout = "layout_with_fr",
  seed = 123
)
}
\arguments{
\item{shapes}{Named character vector, node \code{type} -> visNetwork shape,
merged over \code{\link[=networkplot_default_shapes]{networkplot_default_shapes()}}.}

\item{shape_default}{Shape for a node whose \code{type} is not in \code{shapes}.}

\item{logfc_palette}{Length >= 2 character vector of colours interpolated
into the diverging node-fill scale (low \code{LogFC} -> ... -> high).}

\item{logfc_limit_fn}{Function \code{(numeric) -> numeric(1)}: given the
vector of node \code{LogFC} values, return the symmetric colour limit
(values are clamped to \code{c(-lim, lim)} before mapping). Default: the
larger of \verb{|p25 of the negatives|} and \verb{|p75 of the positives|}.}

\item{highlight_degree}{Nodes with \code{degree >= highlight_degree} get the
highlight border/width; \code{NULL} disables highlighting (every node uses
the defaults).}

\item{highlight_border, border_default}{Node border colour for
high-degree nodes vs. the rest.}

\item{highlight_border_width, border_width_default}{Node border width for
high-degree nodes vs. the rest.}

\item{edge_colour}{Colour for every edge.}

\item{edge_width}{\code{NULL} for constant-width edges (the default), or a
list \code{list(column, scale, invert)} mapping a per-edge column to
visNetwork's \code{width}: \code{column} is the edge-data column to read
(e.g. \code{"cost"} or \code{"weight"}), \code{scale} a function \code{(numeric) -> numeric} producing widths, \code{invert} (default \code{FALSE}) negates the
column first so that \emph{small} values render \emph{bold} (use \code{TRUE} for
\code{"cost"}, where lower cost = stronger interaction).}

\item{node_font_size, legend_font_size}{Font sizes.}

\item{layout, seed}{\code{visIgraphLayout()} layout name and its random seed.}
}
\value{
A list of class \code{"networkplot_theme"}.
}
\description{
Every visual choice \code{plot_network()} makes is a field on the list this
returns. Call it with no arguments for the standard look; override a
field by name to change just that. \code{shapes} is \emph{merged} over the default
map (an override for \code{Kinase} leaves every other type's shape alone), so
the "config file" is really this one R constructor -- a
\code{read_networkplot_theme()} reader for YAML/JSON could be layered on later
without changing \code{plot_network()}.
}
